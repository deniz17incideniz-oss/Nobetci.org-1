import type { Institution } from "@/types/institution";

export async function fetchHospitals(): Promise<Institution[]> {
  // Sağlık Bakanlığı açık veri veya izinli resmi kaynak entegrasyonu için ayrılmıştır.
  return [];
}

